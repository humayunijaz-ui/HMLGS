<?php
/**
 * DEPRECATED / NOT USED.
 *
 * SMTP and mail settings for this project live directly in
 * config/config.php (SMTP_HOST, SMTP_PORT, SMTP_ENCRYPTION,
 * SMTP_USERNAME, SMTP_PASSWORD, SMTP_FROM_EMAIL, SMTP_FROM_NAME,
 * SITE_URL). Edit those instead - this file is intentionally left
 * empty (no constants defined) so it can never be required
 * alongside config.php without causing a "constant already
 * defined" error.
 */
